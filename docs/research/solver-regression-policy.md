# Solver regression policy

Do not rebalance gameplay solely because a search implementation hits a computational ceiling. First improve the solver so it can distinguish infeasibility from search explosion. Only then change region content, health economy, surge timing, or resource placement if the improved solver proves the campaign has no acceptable route.
